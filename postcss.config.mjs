/**
 * PostCSS configuration for Saras luxury fitness application
 * Configures Tailwind CSS and autoprefixer for cross-browser compatibility
 */

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
